<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.solo.asset.mapper.AssetMapper">

    <select id="getAssetData" parameterType="java.lang.String" resultType="org.solo.asset.domain.AssetVO">
        SELECT * FROM userAsset WHERE userId = #{userId}
    </select>
</mapper>