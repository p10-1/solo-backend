<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.solo.product.mapper.ProductMapper">
    <insert id="fetchProducts" parameterType="org.solo.product.domain.ProductVO">
        INSERT INTO product (dclsMonth, korCoNm, finPrdtNm, joinWay, mtrtInt, spclCnd, joinMember, etcNote, type)
        VALUES (#{dclsMonth}, #{korCoNm}, #{finPrdtNm}, #{joinWay}, #{mtrtInt}, #{spclCnd}, #{joinMember}, #{etcNote}, #{type})
    </insert>

    <select id="findByToken" parameterType="map" resultType="java.lang.Integer">
        SELECT count(*)
        FROM product
        WHERE finPrdtNm = #{finPrdtNm}
          AND mtrtInt = #{mtrtInt}
          AND etcNote = #{etcNote}
          AND type = #{type}
    </select>
</mapper>