<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.solo.mypage.mapper.MypageMapper">

    <insert id="insertUserData" parameterType="org.solo.mypage.domain.MypageVO">
        INSERT INTO userData (userID, cash, stock, property, deposit, consume)
        VALUES (#{userID}, #{cash}, #{stock}, #{property}, #{deposit}, #{consume})
    </insert>

    <select id="findUserData" parameterType="String" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM userdata
        WHERE userID = #{userID}
    </select>

    <update id="updateUserData" parameterType="org.solo.mypage.domain.MypageVO">
        UPDATE userdata
        SET consume = #{consume},
            cash = #{cash},
            stock = #{stock},
            property = #{property},
            deposit = #{deposit}
        WHERE userID = #{userID}
    </update>

</mapper>
